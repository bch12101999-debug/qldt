DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/hiepbinh_khieunai?schema=public"
JWT_SECRET="doi-secret-nay-khi-len-production"
PORT=4000
CORS_ORIGIN="http://localhost:5173"

# Dùng để chạy `npm run ensure-admin` — tạo/khôi phục tài khoản admin đầu tiên trên production.
# Không đặt gì cũng được: mặc định sẽ tạo admin@hiepbinh.gov.vn / 123456 (đổi mật khẩu này
# ngay sau khi đăng nhập lần đầu, qua Quản trị hệ thống > Quản lý người dùng > Reset MK).
# ADMIN_EMAIL="admin@hiepbinh.gov.vn"
# ADMIN_PASSWORD="123456"
# Đặt "true" nếu muốn ép đặt lại mật khẩu cho tài khoản admin đã tồn tại (VD khi quên mật khẩu)
# ADMIN_RESET_PASSWORD="false"
